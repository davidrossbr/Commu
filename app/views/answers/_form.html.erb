<%= simple_form_for(@answer, html: {class: "form-horizontal"}) do |f| %>
  <%= f.error_notification %>

  <%# if user_signed_in? %>
	<div>
	<%= f.input :comment_id %>
	<%= f.input :user_id %>
	<%= f.input :content %>
	</div>

	<div class="form-actions">
	<%= f.button :submit %>
	</div>
  <%# end %>
<% end %>
